@model BusinessViewModel
@{
    ViewData["Title"] = Model.FormData.PageTitle;
}
<form asp-action="Create">
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-12 centered-column">
                @Html.Partial(Model.FormData.HeadingPartial)
            </div>
        </div>
    </div>
    <div class="form-horizontal">
        @Html.HiddenFor(model => model.Listing.Categories[0]);
        <hr />
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        @*TODO: make view more generic and add if string.null to check model for which fields to display*@
        @if (!string.IsNullOrWhiteSpace(Model.FormData.Title0Label))
        {
            <div class="form-group">
                <label class="col-md-4 control-label">@Model.FormData.Title0Label</label>
                <div class="col-md-8">
                    <input asp-for="Listing.Titles[0]" class="form-control" />
                    <span asp-validation-for="Listing.Titles[0]" class="text-danger"></span>
                </div>
            </div>
        }

        @if (!string.IsNullOrWhiteSpace(Model.FormData.Title1Label))
        {
            <div class="form-group">
                <label class="col-md-4 control-label">@Model.FormData.Title1Label</label>
                <div class="col-md-8">
                    <input asp-for="Listing.Titles[1]" class="form-control" />
                    <span asp-validation-for="Listing.Titles[1]" class="text-danger"></span>
                </div>
            </div>
        }

        @if (Model.Listing.BusinessType == BusinessType.Information || Model.Listing.BusinessType == BusinessType.Other)
        {
            <div class="form-group">
                <label class="col-md-4 control-label">Please select a category for better searching:</label>
                <div class="col-md-8">
                    @Html.DropDownListFor(model => model.Listing.Categories[1], Html.GetEnumSelectList(typeof(BusinessSearchCategory)), new { @class="form-control"})
                    @*<select asp-for="Listing.Categories[1]" class="form-control" asp-items="@Html.GetEnumSelectList<Looking2.Web.Domain.BusinessSearchCategory>()"></select>*@
                    <span asp-validation-for="Listing.Categories[1]" class="text-danger"></span>
                </div>
            </div>
        }

        @if (Model.Listing.BusinessType == BusinessType.ServiceProviders)
        {
            <div class="form-group">
                <label class="col-md-4 control-label">Please select a category for better searching:</label>
                <div class="col-md-8">
                    @Html.DropDownListFor(model => model.Listing.Categories[1], Html.GetEnumSelectList(typeof(ServiceCategory)), new { @class = "form-control" })
                    @*<select asp-for="Listing.Categories[1]" class="form-control" asp-items="@Html.GetEnumSelectList<Looking2.Web.Domain.BusinessSearchCategory>()"></select>*@
                    <span asp-validation-for="Listing.Categories[1]" class="text-danger"></span>
                </div>
            </div>
        }

        @if (!string.IsNullOrWhiteSpace(Model.FormData.Description0Label))
        {
            <div class="form-group">
                <label class="col-md-4 control-label">@Model.FormData.Description0Label</label>
                <div class="col-md-8">
                    <input asp-for="Listing.Descriptions[0]" class="form-control" />
                    <span asp-validation-for="Listing.Descriptions[0]" class="text-danger"></span>
                </div>
            </div>
        }

        @if (!string.IsNullOrWhiteSpace(Model.FormData.Contact0Label))
        {
            <div class="form-group">
                <label class="col-md-4 control-label">@Model.FormData.Contact0Label</label>
                <div class="col-md-8">
                    <input asp-for="Listing.Contact[0]" class="form-control" />
                    <span asp-validation-for="Listing.Contact[0]" class="text-danger"></span>
                </div>
            </div>
        }

        @if (!string.IsNullOrWhiteSpace(Model.FormData.Contact1Label))
        {
            <div class="form-group">
                <label class="col-md-4 control-label">@Model.FormData.Contact1Label</label>
                <div class="col-md-8">
                    <input asp-for="Listing.Contact[1]" class="form-control" />
                    <span asp-validation-for="Listing.Contact[1]" class="text-danger"></span>
                </div>
            </div>

        }

        @if (!string.IsNullOrWhiteSpace(Model.FormData.Description1Label))
        {
            <div class="form-group">
                <label class="col-md-4 control-label">@Model.FormData.Description1Label</label>
                <div class="col-md-8">
                    <input asp-for="Listing.Descriptions[1]" class="form-control" />
                    <span asp-validation-for="Listing.Descriptions[1]" class="text-danger"></span>
                </div>
            </div>
        }

        @if (!string.IsNullOrWhiteSpace(Model.FormData.Description2Label))
        {
            <div class="form-group">
                <label class="col-md-4 control-label">@Model.FormData.Description2Label</label>
                <div class="col-md-8">
                    <input asp-for="Listing.Descriptions[2]" class="form-control" />
                    <span asp-validation-for="Listing.Descriptions[2]" class="text-danger"></span>
                </div>
            </div>
        }

        @if (!string.IsNullOrWhiteSpace(Model.FormData.Description3Label))
        {
            <div class="form-group">
                <label class="col-md-4 control-label">@Model.FormData.Description3Label</label>
                <div class="col-md-8">
                    <input asp-for="Listing.Descriptions[3]" class="form-control" />
                    <span asp-validation-for="Listing.Descriptions[3]" class="text-danger"></span>
                </div>
            </div>
        }

        @if (!string.IsNullOrWhiteSpace(Model.FormData.Description4Label))
        {
            <div class="form-group">
                <label class="col-md-4 control-label">@Model.FormData.Description4Label</label>
                <div class="col-md-8">
                    <input asp-for="Listing.Descriptions[4]" class="form-control" />
                    <span asp-validation-for="Listing.Descriptions[4]" class="text-danger"></span>
                </div>
            </div>
        }

        @if (!string.IsNullOrWhiteSpace(Model.FormData.Description5Label))
        {
            <div class="form-group">
                <label class="col-md-4 control-label">@Model.FormData.Description5Label</label>
                <div class="col-md-8">
                    <input asp-for="Listing.Descriptions[5]" class="form-control" />
                    <span asp-validation-for="Listing.Descriptions[5]" class="text-danger"></span>
                </div>
            </div>
        }

        @if (!string.IsNullOrWhiteSpace(Model.FormData.Description6Label))
        {
            <div class="form-group">
                <label class="col-md-4 control-label">@Model.FormData.Description6Label</label>
                <div class="col-md-8">
                    <input asp-for="Listing.Descriptions[6]" class="form-control" />
                    <span asp-validation-for="Listing.Descriptions[6]" class="text-danger"></span>
                </div>
            </div>
        }

        @if (!string.IsNullOrWhiteSpace(Model.FormData.Description7Label))
        {
            <div class="form-group">
                <label class="col-md-4 control-label">@Model.FormData.Description7Label</label>
                <div class="col-md-8">
                    <input asp-for="Listing.Descriptions[7]" class="form-control" />
                    <span asp-validation-for="Listing.Descriptions[7]" class="text-danger"></span>
                </div>
            </div>
        }

        @if (!string.IsNullOrWhiteSpace(Model.FormData.Description8Label))
        {
            <div class="form-group">
                <label class="col-md-4 control-label">@Model.FormData.Description8Label</label>
                <div class="col-md-8">
                    <input asp-for="Listing.Descriptions[8]" class="form-control" />
                    <span asp-validation-for="Listing.Descriptions[8]" class="text-danger"></span>
                </div>
            </div>
        }

        @if (!string.IsNullOrWhiteSpace(Model.FormData.Description9Label))
        {
            <div class="form-group">
                <label class="col-md-4 control-label">@Model.FormData.Description9Label</label>
                <div class="col-md-8">
                    <input asp-for="Listing.Descriptions[9]" class="form-control" />
                    <span asp-validation-for="Listing.Descriptions[9]" class="text-danger"></span>
                </div>
            </div>
        }

        @if (!string.IsNullOrWhiteSpace(Model.FormData.BragLabel))
        {
            <div class="form-group">
                <label class="col-md-4 control-label">@Model.FormData.BragLabel</label>
                <div class="col-md-8">
                    <input asp-for="Listing.Brag" class="form-control" />
                    <span asp-validation-for="Listing.Brag" class="text-danger"></span>
                </div>
            </div>
        }
        <p>After you finish with the (optional) field below, click <em>Here</em> to add your location information</p>
        <p>Tell Us More (you might want to copy-and-paste the About-Us page from the website, or review - with attribution);</p>
        <div class="form-group">
            <label class="col-md-2"></label>
            <div class="col-md-10">
                <textarea rows="5" cols="300" asp-for="Listing.LongDescription" class="form-control"></textarea>
                <span asp-validation-for="Listing.LongDescription" class="text-danger"></span>
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
    @Html.HiddenFor(model => model.Listing.BusinessType)
    @Html.HiddenFor(model => model.Listing.Location[0])
</form>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}